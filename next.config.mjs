/** @type {import('next').NextConfig} */
const nextConfig = {
    // output: "export", // Commented for development
    // basePath: "/Portfolio", // Commented to serve from root
    images: {
        unoptimized: true,
    },
};

export default nextConfig;
